#include <iostream>
using namespace std;

typedef struct{         //membuat struct dari stack yang berisi count
    int count;          //ada count bertipe int
    int item[MAXSTACK]; //ada item buat simpan data nya dalam maxstack
} stack;

stack tumpukan;         //membuat tumpukan bertipe stack

void buatstack(stack*x){    //untuk membuat sebuah stack
     x->count=0;
}
void push(){
    if(x->count==MAXSTACK){
        
    }
}

int main(){
    int n;
    cout << "Masukkan n: " << endl;
}

